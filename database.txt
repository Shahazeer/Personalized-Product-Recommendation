USE RECOMMENDATION;

CREATE TABLE TAG(
	TAG_ID INT PRIMARY KEY
);

INSERT INTO TAG VALUES(1);
INSERT INTO TAG VALUES(2);
INSERT INTO TAG VALUES(3);
INSERT INTO TAG VALUES(4);
INSERT INTO TAG VALUES(5);
INSERT INTO TAG VALUES(6);

CREATE TABLE PRODUCT_TAG (
    PRODUCT_ID INT PRIMARY KEY,
    TAG_ID INT,
    FOREIGN KEY (TAG_ID) REFERENCES TAG(TAG_ID)
);

INSERT INTO PRODUCT_TAG VALUES(1,1);
INSERT INTO PRODUCT_TAG VALUES(2,1);
INSERT INTO PRODUCT_TAG VALUES(3,1);
INSERT INTO PRODUCT_TAG VALUES(4,2);
INSERT INTO PRODUCT_TAG VALUES(5,2);
INSERT INTO PRODUCT_TAG VALUES(6,2);
INSERT INTO PRODUCT_TAG VALUES(7,3);
INSERT INTO PRODUCT_TAG VALUES(8,3);
INSERT INTO PRODUCT_TAG VALUES(9,3);
INSERT INTO PRODUCT_TAG VALUES(10,4);
INSERT INTO PRODUCT_TAG VALUES(11,4);
INSERT INTO PRODUCT_TAG VALUES(12,4);
INSERT INTO PRODUCT_TAG VALUES(13,5);
INSERT INTO PRODUCT_TAG VALUES(14,5);
INSERT INTO PRODUCT_TAG VALUES(15,5);
INSERT INTO PRODUCT_TAG VALUES(16,6);
INSERT INTO PRODUCT_TAG VALUES(17,6);
INSERT INTO PRODUCT_TAG VALUES(18,6);

SELECT * FROM PRODUCT_TAG;

CREATE TABLE RECOMMEND_A(
    PRODUCT_ID INT,
    TAG_ID INT,
    INCR_TAG INT,
    PRIMARY KEY (PRODUCT_ID, TAG_ID),
    FOREIGN KEY (PRODUCT_ID) REFERENCES PRODUCT_TAG(PRODUCT_ID),
    FOREIGN KEY (TAG_ID) REFERENCES TAG(TAG_ID)
);

CREATE TABLE RECOMMEND_B(
    PRODUCT_ID INT,
    TAG_ID INT,
    INCR_TAG INT,
    PRIMARY KEY (PRODUCT_ID, TAG_ID),
    FOREIGN KEY (PRODUCT_ID) REFERENCES PRODUCT_TAG(PRODUCT_ID),
    FOREIGN KEY (TAG_ID) REFERENCES TAG(TAG_ID)
);

CREATE TABLE RECOMMEND_C(
    PRODUCT_ID INT,
    TAG_ID INT,
    INCR_TAG INT,
    PRIMARY KEY (PRODUCT_ID, TAG_ID),
    FOREIGN KEY (PRODUCT_ID) REFERENCES PRODUCT_TAG(PRODUCT_ID),
    FOREIGN KEY (TAG_ID) REFERENCES TAG(TAG_ID)
);


SELECT * FROM RECOMMEND_A;
SELECT * FROM RECOMMEND_B;
SELECT * FROM RECOMMEND_C;


DROP TABLE CATEGORIES;
DROP TABLE PRODUCT_TAG;
DROP TABLE INTERACTIONS;
DROP TABLE RECOMMEND_A; 
DROP TABLE RECOMMEND_B; 
DROP TABLE RECOMMEND_C; 